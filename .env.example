DB_USER=root
DB_PASSWORD=your_password_here
DB_HOST=localhost
DB_PORT=3306
DB_NAME=hr_employees_db
CLEAN_DB_NAME=hr_employees_cleaned_db

# Optional: only needed on Android/Termux/Pydroid3 to save charts to
# device storage. Leave unset on PC/Mac/Linux — a local "visualizations"
# folder next to the script will be used automatically.
# OUTPUT_DIR=/sdcard/Termux
